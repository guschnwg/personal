{
  "name": "front",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "parcel watch --out-dir ./build --public-url /static/ --no-cache --hmr-port 37735 --no-source-maps index.html",
    "build": "parcel build --out-dir ./build --public-url /static/ --no-cache index.html"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^5.11.4",
    "@testing-library/react": "^11.1.0",
    "@testing-library/user-event": "^12.1.10",
    "@types/jest": "^26.0.15",
    "@types/node": "^12.0.0",
    "@types/react": "^17.0.0",
    "@types/react-dom": "^17.0.0",
    "parcel-bundler": "^1.12.5",
    "typescript": "^4.1.2",
    "web-vitals": "^2.1.0"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
